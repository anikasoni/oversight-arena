[project]
name = "oversight-arena"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["fastapi", "uvicorn", "pytest", "pydantic", "requests", "scikit-learn"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
